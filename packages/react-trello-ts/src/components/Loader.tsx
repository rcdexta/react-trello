import React from "react";
import { LoaderDiv, LoadingBar } from "../styles/Loader";

export const Loader = () => (
	<LoaderDiv>
		<LoadingBar />
		<LoadingBar />
		<LoadingBar />
		<LoadingBar />
	</LoaderDiv>
);
